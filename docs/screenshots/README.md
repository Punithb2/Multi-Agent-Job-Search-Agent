# Screenshots

Drop screenshots here with these exact names so the main README picks them up:

- `search.png`   - the Discover / search screen
- `matches.png`  - ranked job cards showing match scores
- `studio.png`   - the tailoring workspace with a generated document
- `saved.png`    - the saved jobs page

Take them at a desktop width (around 1440px) for a clean look.
